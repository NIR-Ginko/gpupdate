(executable
  (name gpoa)
  (modules gpoa)
  (modes byte exe)
  (flags (:standard -w -33))
  (libraries gpupdate cmdliner core))

(executable
  (name gpupdate)
  (modules gpupdate)
  (modes byte exe)
  (libraries gpupdate))

(executable
  (name storage)
  (modules storage)
  (modes byte exe)
  (libraries gpupdate))

